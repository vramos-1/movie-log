spring.application.name=demo
spring.jpa.open-in-view=false

# Local development defaults. Override with environment variables in production.
security.jwt.secret=${JWT_SECRET:replace-with-a-strong-secret-key-of-at-least-32-chars}
security.jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}
